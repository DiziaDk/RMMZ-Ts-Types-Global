# RPG Maker MZ TypeScript Boilerplate (Global Scope)

**Область:** Глобальная (Без модулей) | **Цель:** ES5/ES6 | **Среда:** RMMZ
[Читать на английском (Read in English)](README.md)

Готовая заготовка (Boilerplate) для написания плагинов RPG Maker MZ на TypeScript в нативной **Глобальной области видимости**.

В отличие от сложных сборок на Webpack, этот шаблон полностью повторяет структуру движка. Включает декларации типов для Ядра RMMZ, PIXI.js, а также популярных плагинов, таких как Alpha ABS Z.

## 📂 Структура проекта

Этот репозиторий задуман как папка `ts`, которую нужно поместить в корень вашего проекта RPG Maker MZ.

```text
ВашПроект/
├── js/
├── img/
├── ts/                 <-- Этот репозиторий
│   ├── types/          <-- Декларации типов (.d.ts)
│   ├── output/         <-- Сюда попадают скомпилированные JS
│   ├── tsconfig.json   <-- Настройки компилятора
│   └── MyPlugin.ts     <-- Ваш исходный код
└── game.rmmzproject
```

## 📦 Включенные декларации типов

Папка `types/` содержит определения, настроенные для глобального доступа:

1.  **RMMZ Core:** Полное покрытие движка (`$gameParty`, `Scene_Map`, `Window_Base` и т.д.).
2.  **PIXI.js:** Адаптированная версия для совместимости с RMMZ (загружается первой).
3.  **Плагины (`types/plugins/`):**
    *   **Alpha ABS Z (ABSZ):** Полные типы для боевой системы от Phoenix KageDesu (пространства имен `KDCore`, `AA`, `uAPI`).
    *   **DKPlugins:** Типы для **DK Game Time** (от DK).
    *   **Dikarier Ecosystem:** Типы для Dikarier Core, WeatherControl и EffectSystem.
    *   *Примечание по EffectSystem:* API является предварительным, так как исходный код пока обфусцирован.

## 🚀 Как использовать

1.  Создайте папку `ts` в корне вашего проекта RPG Maker.
2.  Склонируйте или распакуйте содержимое этого репозитория в папку `ts`.
3.  Создайте свой `.ts` файл (например, `MyNewPlugin.ts`) внутри этой папки.
4.  Запустите компиляцию TypeScript (`tsc` или build task в VS Code).
5.  Готовый плагин появится в папке `output/`.

## ⚙️ Настройки

Файл `tsconfig.json` уже настроен для корректной работы:
*   Компиляция в **ES5** (стандарт RMMZ).
*   Отключены модули (работа в глобальном контексте).
*   **PIXI типы загружаются первыми** во избежание конфликтов зависимостей.
*   Результат компиляции сохраняется в `./output`.

## Лицензия

MIT License.